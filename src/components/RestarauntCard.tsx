import { View, Text } from "react-native";
import React from "react";

const RestarauntCard = ({
  id,
  imgUrl,
  title,
  rating,
  genre,
  address,
  short_description,
  dishes,
  long,
  lat,
}) => {
  return (
    <View>
      <Text>RestarauntCard</Text>
    </View>
  );
};

export default RestarauntCard;
